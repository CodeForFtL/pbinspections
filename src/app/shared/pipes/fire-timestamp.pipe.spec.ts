import { FireTimestampDatePipe } from './fire-timestamp-date.pipe';

describe('FireTimestampDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FireTimestampDatePipe();
    expect(pipe).toBeTruthy();
  });
});
